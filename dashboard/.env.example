# Dashboard client env (Vercel)
# Set these in your Vercel project settings under Environment Variables.
# The VITE_ prefix makes them available at build time in the browser bundle.

# Supabase connection (Railway-hosted recommended to avoid $20 IPv4 add-on)
VITE_SUPABASE_URL=https://YOUR_SUPABASE_HOST
VITE_SUPABASE_ANON_KEY=YOUR_SUPABASE_ANON_KEY

# Optional: public URL used by alert links
VITE_DASHBOARD_PUBLIC_URL=https://your-dashboard.vercel.app
